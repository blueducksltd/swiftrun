"use client";
import { FormEvent, useState } from "react";
import { FiCheck, FiX } from "react-icons/fi";
export default function CareerApplicationModal({ careerId, careerTitle, onClose }: { careerId: string; careerTitle: string; onClose: () => void }) { const [submitted, setSubmitted] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setBusy(true); const response = await fetch('/api/applications', { method: 'POST', body: new FormData(event.currentTarget) }); if (response.ok) setSubmitted(true); else setError((await response.json()).error || 'Application could not be submitted'); setBusy(false) }; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-5"><div className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-6 sm:p-8"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#066ac0]">Career application</p><h2 className="mt-2 font-heading text-2xl font-bold">{submitted ? 'Application received' : `Apply for ${careerTitle}`}</h2></div><button onClick={onClose} aria-label="Close application"><FiX /></button></div>{submitted ? <div className="py-12 text-center"><FiCheck className="mx-auto text-4xl text-[#267d73]" /><p className="mt-4 text-sm text-[#52656b]">Thanks for applying. Our team will review your application.</p><button onClick={onClose} className="mt-6 rounded-xl bg-[#066ac0] px-5 py-3 text-sm font-bold text-white">Close</button></div> : <form onSubmit={submit} className="mt-7 grid gap-4"><input type="hidden" name="careerId" value={careerId} /><input type="hidden" name="careerTitle" value={careerTitle} />{[['name', 'Full name', 'Jane Doe'], ['email', 'Email address', 'jane@example.com'], ['phone', 'Phone number', '+234...']].map(([name, label, placeholder]) => <label key={name} className="text-sm font-semibold">{label}<input required type={name === 'email' ? 'email' : 'text'} name={name} placeholder={placeholder} className="mt-2 w-full rounded-xl border border-[#d9e5e1] px-4 py-3 font-normal outline-none focus:border-[#066ac0]" /></label>)}<label className="text-sm font-semibold">CV <input required type="file" name="cv" accept=".pdf,.doc,.docx" className="mt-2 w-full rounded-xl border border-dashed border-[#b7cfc8] px-3 py-3 text-sm font-normal" /></label><label className="text-sm font-semibold">Cover letter <textarea name="coverLetter" rows={5} placeholder="Tell us why you are a strong fit" className="mt-2 w-full rounded-xl border border-[#d9e5e1] px-4 py-3 font-normal" /></label>{error && <p className="rounded-lg bg-[#fff0ed] p-3 text-sm text-[#b34e43]">{error}</p>}<button disabled={busy} className="rounded-xl bg-[#066ac0] px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{busy ? 'Submitting...' : 'Submit application'}</button></form>}</div></div> }
